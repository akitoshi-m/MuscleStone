    <%= form_for(:session, url: login_path) do |f| %>
      <%= f.hidden_field :email, value: 'guestuser@aaa.com' %>
      <%= f.hidden_field :password, value: 'guestuser12' %>
      <%= f.submit 'ゲストユーザーではじめてみる', class: 'btn btn-arrange btn-green btn-block' %>
    <% end %>