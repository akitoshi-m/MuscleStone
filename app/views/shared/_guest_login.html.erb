    <%= form_for(:session, url: login_path) do |f| %>
      <%= f.hidden_field :email, value: 'guest@guest.com' %>
      <%= f.hidden_field :password, value: 'password12' %>
      <%= f.submit 'ゲストユーザーではじめてみる', class: 'btn btn-arrange btn-green btn-block' %>
    <% end %>