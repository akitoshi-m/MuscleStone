    <%= form_for(:session, url: login_path) do |f| %>
      <%= f.hidden_field :email, value: 'guest@guest.com' %>
      <%= f.hidden_field :password, value: 'guest1234' %>
      <%= f.submit 'テストユーザーではじめてみる', class: 'btn btn-arrange btn-green btn-block' %>
    <% end %>